<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout android:id="@+id/add_sub_task_container"
                xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:app="http://schemas.android.com/apk/res-auto"
                android:layout_width="match_parent"
                android:layout_height="@dimen/add_sub_task_layout_height">

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_alignParentTop="true"
        android:contentDescription="@null"
        android:src="@drawable/note_detail_divider"/>

    <android.support.v7.widget.CardView
        android:id="@+id/add_item_container"
        android:layout_width="360dp"
        android:layout_height="35dp"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="10dp"
        android:foreground="?android:attr/selectableItemBackground"
        app:cardBackgroundColor="#1E000000"
        app:cardElevation="0dp">

        <ImageView
            android:id="@+id/add_item"
            android:layout_width="17dp"
            android:layout_height="17dp"
            android:layout_gravity="center"
            android:clickable="false"
            android:contentDescription="@string/string_add_new_item"
            android:src="@drawable/ic_plus"/>
    </android.support.v7.widget.CardView>

</RelativeLayout>